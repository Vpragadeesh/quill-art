// #ifndef MAINWINDOW_HPP
// #define MAINWINDOW_HPP

// #include<imgui.h>
// // #include<GLFW/glfw3.h>

// // MainWindow encapsulates the main UI (dockspace, menubar, toolbar) inspired by
// // zide.
// class MainWindow {
// public:
//   // Pass the GLFW window pointer so we can call glfw functions when needed.
//   explicit MainWindow(GLFWwindow *window);
//   ~MainWindow();

//   // Call this once per frame to render the UI.
//   void Render();

// private:
//   GLFWwindow *m_window;
// };

// #endif // MAINWINDOW_HPP